# Cartesian Authority Atomic Cutover Record

Status: completed transition record. This page is evidence and rollback
guidance, not a second authority source.

## Identity

- Authorized parent:
  `a8076f2fada691c0620e5ff7e046fb0aa4e075c7`.
- Cutover commit: this commit.
- Reviewed semantic baseline:
  `9b283e16cf75b3264b00bc3f34d15e21bbb92a96`.
- Reviewed candidate SHA-256:
  `30cf4ed840b00c09da39ba4e15b3cb6c3d2c1376263877a2e7778f5a15bef716`.
- Promoted authority SHA-256:
  `6057ef50fd48ff329c9e7226665ea29f59ae4e35db3f43cb71bf29f5c4c2175e`.
- Inventory: `232` records, `44` hashed documents, and `150` derived
  execution IDs.
- Generated registry SHA-256:
  `ac4313cf1d8b332c08367f02251d5fc1e460ea1fb32dcec45cb87dd18e39ba70`.
- Generated marked-whitelist SHA-256:
  `639e9538d6d1db5f48af67fbfd15857a1bc7b3c4316d5c26c2f1ff070b6cc3d6`.

Pass 394 independently reviewed every record and corrected two confined
source-surface omissions before final parity. Pass 395 independently reviewed
the atomic transaction. User authorization explicitly pinned the parent above.

## Result

`authority.toml` is now the sole record-level authority. Exact promotion kept
schema version `3`, all documents and records byte-for-byte, and changed only:

```toml
artifact_kind = "cartesian_authority"
authoritative = true
authorization_complete = true
```

The complete generated `registry.md` and marked `AGENTS.md` whitelist are
one-way checked views. Manually maintained policy, `current.md`, `invariants.md`,
and canonical contracts may restrict machine authority but never broaden it.
Missing facts, stale views, conflicts, or checker failures fail closed.

The permanent `docs/check_cartesian_authority.jl` owns schema, path, document
hash, heading, lifecycle/grant, dependency, execution-membership, deterministic
render, committed-view, legacy-file, and concurrent-mutation validation. Its
only modes are `--check`, `--self-test`, and external-only `--render DIR`.

The transition shadow, candidate, snapshot, reverse parser, rehearsal writer,
and compatibility checkers were removed. Git history and the Pass 394 review
retain the old prose evidence; no second tracked archive was created.

## Activation

Activation is the ordinary fast-forward of this complete commit from the exact
authorized parent to `origin` `refs/heads/main`. A dirty worktree, generated
files, temporary render, detached review commit, or other ref is not authority.

The required `cartesian-authority` GitHub job runs the permanent check and
self-test with read-only contents permission. The docs job depends on it.
`docs/make.jl` does not duplicate the authority gate.

## Validation

Before activation the transaction must pass:

- exact `232/44/150` parity and promoted digest;
- all six legacy checks at the authorized parent;
- permanent `--check` and `--self-test`;
- two byte-identical fresh external renders and committed-view equality;
- unique record headings, document links, and legacy-absence scans;
- focused docs tests and local Documenter;
- exact staged scope, `git diff --cached --check`, and the carrying-cost gate;
- a second detached-checkout replay of checker, tests, docs, and rendering;
- whole-commit revert proof whose index tree equals the authorized parent tree.

No source, scientific contract, numerical test surface, artifact, driver,
solver, or canonical hashed document changed.

## Rollback

Rollback is whole-commit only. Before activation, discard this commit. After
activation, revert this complete cutover commit and verify that the resulting
tree equals the authorized parent and all six legacy checks pass. Never repair
by flipping flags, restoring one prose view, retaining mixed checkers, or using
last-known-good generated bytes as authority.

After later authority amendments, rollback requires a separately reviewed
whole-tree reconciliation rather than restoring stale prose.
