# Old Flat Cartesian Retirement Audit

Read-only audit requested during pass 234. No files were edited and no tests
were run by the audit.

## Safe Soon

### PQS/PQS/product route-shaped density-density fixture path

- Source: `src/cartesian_contracted_parent_metrics/current_route_metadata_export.jl`
  around the route-shaped source-shell/source-mode inventory and producer
  helpers.
- Test pressure: `test/nested/pqs_projected_q_shell_local_layer_integration_runtests.jl`
  in the slow integration runner.
- Classification: deletion candidate / route-shadow fixture.
- Superseded by: CPB/provider-style local operator blocks plus current
  independent-H2 route-owned support/source-plan direction.
- Use: best near-term line-budget funding target, but as a separate cleanup
  pass, not bundled into support-plan implementation.

### PQS/PQS/product raw-box density-density route producer fixture

- Source: `src/cartesian_contracted_parent_metrics/current_route_metadata_export.jl`
  producer and IDA-provenance route-shaped variants.
- Test pressure: slow projected-q-shell integration helper assertions.
- Classification: deletion candidate after preserving any still-needed density
  convention in a compact CPB/source-box contract test.
- Use: separate cleanup pass.

### Excess diagnostic assertions in projected-q-shell integration test

- File:
  `test/nested/pqs_projected_q_shell_local_layer_integration_runtests.jl`.
- Evidence: included by `test/nested/integration_runtests.jl`, not default.
- Classification: deletion candidate for metadata/nonclaim blocks, helper-name
  assertions, and route-shadow vocabulary.
- Preserve only compact math checks not covered elsewhere: boundary retained
  counts, density normalization convention, and nuclear charge/sign convention.

## Audit Before Delete

### Product/doside source-box density/nuclear helpers

- Source:
  `src/cartesian_contracted_parent_metrics/legacy_source_box_fixtures.jl` and
  `src/cartesian_contracted_parent_metrics/source_box_pair_shadow.jl`.
- Evidence: source callers remain in `current_route_metadata_export.jl`.
- Classification: temporary oracle/reference, then deletion candidate after the
  route-shaped density-density producer/consumer cleanup.
- Do not bundle with current H2 support-plan work.

### Current-route metadata export

- Source:
  `src/cartesian_contracted_parent_metrics/current_route_metadata_export.jl`.
- Evidence: used by `pqs_source_metadata_real_artifact_acceptance_support.jl`
  and slow integration runner.
- Classification: temporary migration scaffold.
- Recommendation: shrink to live source metadata only; do not delete wholesale.

### Atom-growth/high-order diatomic planning tests

- Source: `src/cartesian_nested_diatomic.jl`.
- Evidence: still used by low-order materialization, ordinary QW nested
  frontends, QW operator-carried spaces, and current shellification/support
  audits.
- Classification: reference/oracle plus migration scaffold.
- Recommendation: revisit after independent H2 support plan becomes authority.

## Keep As Oracle / Reference For Now

### Fake-PQS H2 463 golden regression

- Files:
  `test/driver_inputs/h2_fake_pqs_q5_wl_source_backed_r4.jl` and
  `test/nested/cartesian_ham_builder_h2_fake_pqs_wl_source_backed_r4_runtests.jl`.
- Classification: reference/golden regression, not deletion target yet.
- Delete only after independent PQS has comparable source-plan/final-basis/H1/
  H1-J/RHF coverage.

### Route-global WL/GTO supplement machinery

- Source:
  `src/cartesian_pair_block_materialization/CartesianPairBlockMaterialization.jl`
  and `route_global_atom_gto_final_basis_route.jl`.
- Classification: active reference/migration implementation, not deletion
  target.
- Important for later MWG/GTO supplement staging.

## Active Implementation, Not Deletion Target

- New layered route spine:
  `CartesianRouteCore`, `CartesianShellification`,
  `CartesianTerminalLowering`, `CartesianRetainedUnits`,
  `CartesianPairOperatorPlans`, and `CartesianPairBlockMaterialization`.
- Independent H2 PQS target/readiness/support-plan work.

Do not use these active implementation surfaces to fund line budget.

## Recommended Next Cleanup

Target the slow `pqs_projected_q_shell_local_layer_integration_runtests.jl`
route-shaped density-density producer/consumer sections and the corresponding
test-only route-shadow helpers. This is the cleanest deletion pool for future
line budget.
