# High-Order Mainline Import Readiness

Date: 2026-05-15

## Purpose

This memo identifies the stable slice of the high-order lane that is ready for
repo-manager to consider importing into mainline soon. The import candidate is
not the full high-order/OPCU research program. It is the dimension-organized
owned-unit formulation needed to replace unsplit shared rectangular molecular
shells by endcap/panel units in the ordinary H2 nested construction.

The strongest current evidence is the old-standard-aligned H2 route: it keeps
the old parent basis, molecular support, and split atomic child/core content,
then replaces only unsplit shared molecular shell layers. That makes the
comparison local to the shell ownership policy instead of changing the whole
molecular basis.

## Definitions

- Owned unit: a retained contraction unit with a declared support region, a
  contraction map, and an assembly role.
- Support mask: the set of parent grid directions owned by a unit. Importable
  endcap/panel units require exact support coverage with no duplicate owners
  inside a replaced shell.
- Contraction map: the coefficient map from owned support directions to
  retained basis functions. For the current import candidate these maps remain
  IDA-safe and have positive contracted weights.
- Assembly role: whether a unit is part of an unsplit shared molecular shell,
  a split atomic child/core region, or another preserved inner component.
- Old shared molecular shell: an ordinary nested rectangular shell layer that
  remains one shared molecular object before the construction splits into
  separate atomic child/core boxes.
- Split atomic child/core content: the existing ordinary nested inner H2
  chemistry content after the molecular source has split. This content is left
  unchanged by the import candidate.
- Endcap/panel shell: a replacement owned-unit shell made from two endpoint
  caps and four side panels. The side panels absorb the four long edge lines,
  so ownership is disjoint.
- `q`: the retained one-dimensional order floor used on each cap/panel
  transverse direction.
- `L`: the retained long-direction length used by each side panel.
- Finite IDA Hamiltonian ED: exact diagonalization of the finite Hamiltonian
  generated by the current IDA operator route. The caveat is the IDA
  interaction representation and finite basis, not that the ED solve is
  somehow not exact for its finite `H,V`.

## Import Candidate

The stable candidate is a small internal producer path for replacing every
unsplit shared rectangular molecular shell by endcap/panel owned units:

```text
retained count = 2*q^2 + 4*q*L
```

The producer should preserve the old shell support exactly, keep ownership
disjoint, and leave split atomic child/core boxes untouched. It should not
expose a broad public OPCU framework yet.

The current recommended default for stretched H2 is all-shared `q=4,L=4`.
This replaces each unsplit shared molecular shell with two `4 x 4` caps and
four `4 x 4` side panels. It is the best current balance between count control
and energy stability. The `q=3,L=3` policy is useful as a low-count stress
case but was too aggressive when all shared shells were replaced. The
`q=5,L=5` policy improves R = 4 energy slightly but costs substantially more
functions.

For future atom-centered high-order diatomic recipes, this import slice should
be interpreted with the official box-policy rule: grow protected atom-local
boxes until contact or near-contact, keep the middle/contact region simple and
shared, and absorb unavoidable parity/spacing/`q` mismatch at the outermost
shared molecular shell whenever possible. The full parent box remains the
default support. A smaller side ladder or cropped parent is a labelled
diagnostic/research request, not the default route.

## Evidence

### One-Shell Replacement

The first stable H2 chemistry result replaced only the first outer shared
shell layer and preserved the old inner content. At R = 4.0 with H/cc-pVTZ
S/P supplement:

| route | outer retained | final dim | ED total | BO error (mHa) |
|---|---:|---:|---:|---:|
| old S/P ordinary | 98 | 481 | -1.015613837691 | 0.776 |
| q=3,L=3 endcap/panel | 54 | 437 | -1.015618411904 | 0.772 |
| q=4,L=4 endcap/panel | 96 | 479 | -1.015659199415 | 0.731 |
| q=5,L=5 endcap/panel | 150 | 533 | -1.015664455292 | 0.726 |

The `q=3,L=3` one-shell replacement achieved the old S/P energy scale with 44
fewer final functions. The `q=4,L=4` row was nearly count-matched to the old
shell and slightly lower in energy.

Source artifacts:

- `tmp/work/h2_old_standard_aligned_endcap_panel_sp_r4_2026-05-14.txt`
- `tmp/work/h2_old_standard_aligned_endcap_panel_q4_l4_sp_r4_2026-05-14.txt`
- `tmp/work/h2_endcap_panel_q_family_vs_ns5_r4_2026-05-14.txt`

### All-Shared R = 4 Family

The next R = 4 test replaced every unsplit shared molecular shell before the
atomic child/core split. The anatomy found two replaceable shared layers. Both
had exact support ownership under the endcap/panel rule; split atomic
child/core boxes were unchanged.

| route | replaced retained | final dim | ED total | BO error (mHa) | delta vs old S/P (mHa) |
|---|---:|---:|---:|---:|---:|
| old S/P ordinary | 98 first shell | 481 | -1.015613837691 | 0.776 | 0.000 |
| all-shared q=3,L=3 | 108 total | 377 | -1.014450269144 | 1.940 | +1.164 |
| all-shared q=4,L=4 | 192 total | 461 | -1.015663743783 | 0.727 | -0.050 |
| all-shared q=5,L=5 | 300 total | 569 | -1.015902995615 | 0.487 | -0.289 |

This is the key count/energy result for choosing `q=4,L=4`: it stays slightly
below the old S/P ordinary ED energy while using fewer final functions at
R = 4. The `q=3,L=3` all-shared replacement saved many functions but lost
about 1.16 mHa relative to old S/P ordinary.

Source artifact:

- `tmp/work/h2_endcap_panel_all_shared_q_family_r4_2026-05-14.txt`

### All-Shared q=4 Stretched Sweep

The all-shared `q=4,L=4` policy was then swept over stretched H2:
R = 2.5, 3.0, 4.0, 5.0, 6.0 bohr, with H/cc-pVTZ S/P supplement. All rows
packaged cleanly, HF converged, ED lowered HF, and ED residuals were clean.

| R | q=4 dim | q=4 ED total | BO error (mHa, if available) |
|---:|---:|---:|---:|
| 2.5 | 449 | -1.093358761692 | 0.579 |
| 3.0 | 449 | -1.056708724750 | 0.618 |
| 4.0 | 461 | -1.015663743783 | 0.727 |
| 5.0 | 527 | -1.002466234506 | unavailable |
| 6.0 | 527 | -0.999400534385 | unavailable |

Source artifact:

- `tmp/work/h2_endcap_panel_all_shared_q4_sp_stretched_sweep_2026-05-14.txt`

### Joined Old-vs-New Stretched Comparison

The matching old S/P ordinary rows were run for the same stretched points and
joined against the all-shared `q=4,L=4` sweep.

| R | old dim | q=4 dim | dim change | old ED total | q=4 ED total | q=4 - old (mHa) |
|---:|---:|---:|---:|---:|---:|---:|
| 2.5 | 501 | 449 | -52 | -1.093151648897 | -1.093358761692 | -0.207 |
| 3.0 | 485 | 449 | -36 | -1.056695835156 | -1.056708724750 | -0.013 |
| 4.0 | 481 | 461 | -20 | -1.015613837691 | -1.015663743783 | -0.050 |
| 5.0 | 579 | 527 | -52 | -1.003418513654 | -1.002466234506 | +0.952 |
| 6.0 | 579 | 527 | -52 | -1.000483345515 | -0.999400534385 | +1.083 |

The dimension savings persisted at every stretched point. The q=4 route was
lower than old S/P ordinary at R = 2.5, 3.0, and 4.0, and higher by about
1 mHa at R = 5.0 and 6.0. Local BO references were available for R = 2.5,
3.0, and 4.0 only; the R = 5.0 and R = 6.0 rows need BO references before
absolute accuracy is interpreted.

Source artifact:

- `tmp/work/h2_old_sp_vs_all_shared_q4_stretched_comparison_2026-05-15.txt`

## What Should Stay Experimental

Do not import the following as part of this mainline slice:

- Atom-centered high-order FSB/FBU work.
- Distorted raw-cube completeness or distorted local-cube claims.
- Arbitrary projected residual shell/core repairs. These broke current IDA
  fixed-block packet assumptions and should not be normalized.
- The broad OPCU framework machinery as a public abstraction.
- Scratch external-target studies, Gaussian-target probes, D-shell target
  builders, and side-wall usefulness tests.
- Any claim that the endcap/panel H2 evidence proves production high-order
  nesting, correction-layer usefulness, DMRG behavior, or larger-molecule
  transferability.

Those threads remain useful research context, but the mainline-ready slice is
only the owned-unit producer for old-standard-aligned shared rectangular shell
replacement.

## Proposed Mainline Shape

Implement this as a small internal or experimental producer path, not as a new
public framework. A reasonable shape is:

- An internal owned-unit specification for rectangular molecular shell layers.
- A support-mask checker that verifies exact coverage, no duplicate ownership,
  and no cap/panel overlap.
- An endcap/panel contraction-map producer parameterized by `q` and `L`.
- A guarded insertion point that applies the producer only to unsplit shared
  molecular shell layers and leaves split atomic child/core boxes unchanged.
- Internal names that make the status explicit, for example
  `experimental_endcap_panel_shared_shell` or similar.

The implementation should reuse the existing ordinary nested fixed-block and
operator machinery. It should not introduce arbitrary post-projection residual
columns or nonpositive contracted weights.

## Test Ladder

The import test ladder should be incremental:

1. Anatomy and support ownership: list every candidate shared shell, confirm
   it is a single rectangular molecular shell, and verify no missing or
   duplicate owned support directions.
2. Fixed-block/operator preflight: verify fixed overlap, positive contracted
   weights, finite `gaussian_sum` and `pair_sum`, operator overlap, and H/V
   symmetry.
3. R = 4 H2 S/P HF/ED: compare old S/P ordinary against all-shared q=4/L=4
   with the same finite IDA Hamiltonian and the same HF/ED route.
4. Stretched H2 regression: run R = 2.5, 3.0, 4.0, 5.0, 6.0 and join against
   old S/P ordinary. Track dimension, ED total, ED-HF lowering, BO error where
   references are available, and residuals.

Passing this ladder would make the internal endcap/panel producer ready for
mainline experimental use. It would not make the broad high-order lane or OPCU
framework production-ready.

## Import Recommendation

Repo-manager can consider importing the owned-unit/endcap-panel shared-shell
producer soon, with `q=4,L=4` as the current default for stretched H2 tests.
The import should be explicitly experimental/internal and should include the
test ladder above. The broader high-order lane should remain out of mainline
until its objects and validation targets are narrowed to similarly stable
slices.
