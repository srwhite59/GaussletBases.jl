Pass 229 post-hoc response: fake-PQS relabel and quarantine

This pass did not execute the previously queued supplement-provider audit in
`blurb.229.md`. The user redirected the lane after identifying that the H2
463 "PQS" endpoint was a WL/QW fixed-source retained-transform reproduction,
not an independent PQS construction.

What changed:
- Commit `208df0ad` renamed the H2 463 endpoint to fake-PQS:
  - `h2_fake_pqs_q5_wl_source_backed_r4.jl`
  - `cartesian_ham_builder_h2_fake_pqs_wl_source_backed_r4_runtests.jl`
- It added explicit `fake_pqs/*` artifact fields, including:
  - `enabled = true`
  - `source = :source_backed_fixed_source_oracle`
  - `independent_pqs_transform = false`
  - `delete_after_independent_pqs = true`
- It relabeled source-plan authority as
  `:fake_pqs_private_source_backed_adapter_authority`.

Corrective relabel:
- Doer review found that the first relabel still let stable fields advertise a
  real endpoint:
  - `physics/endpoint_ready == true`
  - route fields still exposed physical-gausslet statuses without same-group
    fake markers.
- Commit `26ecc737` corrected this:
  - `physics_endpoint_ready = false`
  - `physics_endpoint_blocker =
    :fake_pqs_source_backed_wl_reproduction_not_independent_pqs`
  - `physics/endpoint_role = :fake_pqs_wl_reproduction`
  - `physics/independent_pqs_transform = false`
  - `route/fake_pqs_enabled = true`
  - `route/comparison_role = :fake_pqs_wl_reproduction`
  - `route/independent_pqs_transform = false`
  - `route/source = :source_backed_fixed_source_oracle`
  - `route/warning =
    :retained_transform_imported_from_wl_qw_fixed_source_oracle`

Validation recorded:
- For `208df0ad`, manager ran:
  - `julia --project=. -e 'using GaussletBases; println("load ok")'`
  - `julia --project=. -e 'using Test; t = @elapsed include("test/nested/cartesian_ham_builder_h2_fake_pqs_wl_source_backed_r4_runtests.jl"); println("elapsed_s=", t)'`
    and it passed `82/82` in about `141.8s` after the final test shrink.
  - `git diff --check`
- For `26ecc737`, doer reported:
  - fake-PQS endpoint test passed `88/88` in about `3m22s`
  - load passed
  - `git diff --check` passed
  Manager reviewed the diff and pushed without rerunning tests.

Line-count note:
- The initial relabel kept `src + test` net-negative by shrinking stale
  assertions in the renamed H2 fake-PQS test.
- For the corrective relabel action, the normal line-reduction rule was
  temporarily ignored. The correction added explicit guard fields and tests in
  the same stable artifact groups that downstream consumers might read. That
  clarity was more important than net-negative arithmetic for this small
  safety fix.

Current interpretation:
- The H2 463 artifact is a fake-PQS source-backed WL/QW reproduction only.
- It is useful as a driver golden regression.
- It is not an independent PQS physics endpoint and must not be used as a
  PQS/WL scientific comparison.
- Future independent PQS work should start from a separate target whose
  artifact says `fake_pqs/enabled = false` and whose retained transform is
  generated by the PQS source-box construction, not imported from the WL/QW
  fixed-source oracle.

-- repo-manager@macmini
