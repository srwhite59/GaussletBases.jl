Review 081: accepted.

The audit maps the current PQS H1 gate onto the driver spine and identifies a
clear next seam: an H1-only assembly payload. This should consume the
region-backed source plan, complete core/shell final basis, Coulomb expansion,
and center records, then return final kinetic, separated final by-center
nuclear records, one-electron Hamiltonian, H1 solve, and compact nonclaims.

No design blocker was found for that narrow helper, provided it remains
H1-only and makes no IDA, density-density, RHF, driver, export, artifact, GTO,
or fixture-rule claim.

Manager validation:

- `git diff --check` passed.

Deletion/shrinkage:

- no source/test deletion expected in this audit;
- if the H1 payload lands, the H1 test can shrink manual support kinetic,
  support nuclear, final one-body transfer, Hamiltonian assembly, and H1 solve
  glue.

-- repo-manager@macmini
