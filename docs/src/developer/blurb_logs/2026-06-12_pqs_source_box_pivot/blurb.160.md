Pass 160 - no-edit audit for diatomic handoff/consumer/readiness duplication

Role: repo-doer@macmini

Task type: no-edit source shrink audit. Do not edit files, do not commit.

Purpose:

Audit the carrying cost introduced by the private diatomic Hamiltonian handoff,
consumer contract, and readiness payloads before any downstream HFDMRG/CR2/HamV6
format work. Pass 159 replaced a real blocker, but it also added another object
that repeats fields from the handoff and readiness layers. We need to decide
what can be consolidated or deleted before adding more.

Corrective line-budget rule for the next implementation pass:

After this audit, any pass that edits `src/` or `test/` in this lane must be
net-line-negative across tracked `src/` + `test/` files unless the manager grants
an explicit exception in advance.

Measure with:

```text
git diff --numstat -- src test
```

Acceptance condition:

```text
sum(deleted) > sum(added)
```

Do not count docs/handoff files. Do not satisfy the rule by deleting accepted
scientific endpoint tests, deleting only whitespace/comments, or moving code to
untracked/tmp files. If the next required source/test change cannot be offset by
deleting stale source/test surfaces in the same pass, doer should write
`ATTENTION.md` with the exact blocker and stop.

Governing policy:

- `AGENTS.md`: every line has carrying cost; if a concept has internal
  structure, carry compact module-owned objects, but do not allow compact
  objects to proliferate into a payload cloud.
- `JuliaStyle.md`: avoid repeated status-flag bundles and flat field copying;
  glue should be thin and directional.
- `docs/src/developer/pqs_source_box_operator_framework.md`: source-box-first
  PQS remains the active frame; support-row contraction is oracle/debug.

Current state:

- `src/pqs_source_box_diatomic_complete_core_shell.jl` is about 3,719 lines.
- The route now has:
  - `_PQSDiatomicCompleteCoreShellHamiltonianHandoffPayload`
  - `_PQSDiatomicCompleteCoreShellHamiltonianConsumerContractPayload`
  - `_PQSDiatomicCompleteCoreShellHamReadinessPayload`
- Pass 159 changed readiness from
  `:missing_diatomic_hamiltonian_consumer_contract` to
  `:missing_hfdmrg_density_density_contract`.

Audit questions:

1. Which fields are duplicated across handoff, consumer contract, readiness
   summary, and metadata?

   Focus on:
   - final dimension;
   - one-body status/reference;
   - density gauge;
   - raw pair-factor convention;
   - support weight count;
   - pre-final pair matrix shape;
   - final-to-pre-final coefficient shape;
   - nuclear/electron/spin metadata;
   - downstream readiness flags;
   - repeated public/export/artifact/HFDMRG/CR2/HamV6 false flags.

2. Which duplication is necessary because it is the active external contract,
   and which is only transitional convenience?

3. Can the consumer contract be made thinner by carrying the handoff reference
   plus a compact readiness summary instead of copying many handoff fields?

4. Can downstream readiness flags be generated by one helper instead of
   repeated in handoff, consumer, readiness, metadata, and tests?

5. Is there any now-superseded blocker or test assertion that can be deleted in
   the next pass?

6. What should be the exact shrink pass after this audit?

7. What source/test line budget should pass 161 target? Report the stale
   surfaces that can pay for any needed edits.

Possible acceptable recommendations:

- Shrink the consumer contract to carry `source_handoff`, status, blocker,
  `readiness`, summary, metadata, and only minimal scalar fields needed by
  readiness/test.
- Add one tiny private helper for the repeated downstream false flags and use it
  in handoff/consumer/readiness summaries.
- Leave the structure as-is only if you can name exact downstream callers that
  require the duplicated fields now. Tests alone are not enough.

Do not implement any recommendation in this pass.

Forbidden:

- No source edits.
- No test edits.
- No new payloads.
- No downstream format work.
- No dense `Vee`, final-space `V`, `V6`, `Vblocks`, H1/J, RHF/SCF, WL, CR2 run,
  HFDMRG run, public API, export/HamV6/JLD2, or artifacts.

Validation:

Read-only audit only. Run:

```text
git status --short --branch
```

Use `rg`, `sed`, and `wc` as needed. Do not run Julia tests.

Report back:

- duplicated field/flag inventory with source line regions;
- which fields/flags should stay and why;
- which fields/flags should be consolidated or deleted;
- recommended pass 161 shrink plan with expected net line reduction;
- proposed `src/` + `test/` line-budget target for pass 161;
- exact blockers if no shrink is safe;
- git status;
- deletion/shrinkage report:
  - deleted: none in this audit
  - simplified: none in this audit
  - quarantined: none in this audit
  - not deleted because: audit only
  - exact remaining caller/blocker:

-- repo-manager@macmini
