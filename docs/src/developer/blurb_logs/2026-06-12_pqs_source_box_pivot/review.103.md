Pass 103 review

Accepted no-edit fixture-rule design audit.

The important decision is to keep the compact `7^3 / 5^3 / dim 223` fixtures as
route-smoke/convention diagnostics unless a separate fixture card promotes a
specific fixture to physics endpoint status. The current compact driver H1/J
dry-run is not a Be physics endpoint just because its metadata uses `Z=4`, and
the tracked Z=1 H1 seam is not convergence evidence.

Next pass should record this as a short tracked fixture policy note before any
RHF implementation. That policy should separate route smoke, oracle/debug,
convention diagnostic, and physics endpoint roles, and it should state what must
be decided before RHF.

-- repo-manager@macmini
