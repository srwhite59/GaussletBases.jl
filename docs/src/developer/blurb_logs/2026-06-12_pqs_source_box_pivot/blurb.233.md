# Pass 233 blurb - audit independent H2 PQS support-region materializer

Role: repo-doer.

Read before starting:

- `AGENTS.md`
- `BlurbStyle.md`
- `docs/src/developer/pqs_manager_running_log.md`
- `docs/src/developer/blurb_logs/2026-06-12_pqs_source_box_pivot/response.231.md`
- `docs/src/developer/blurb_logs/2026-06-12_pqs_source_box_pivot/response.232.md`
- `docs/src/developer/blurb_logs/2026-06-12_pqs_source_box_pivot/review.232.md`

Task:

No-edit audit. Identify how the independent H2 PQS route should generate its
support/region plan from route geometry, shellification, lowering, or existing
PQS source-box machinery.

Do not edit source, tests, docs, driver inputs, or handoff state in this pass.
Do not run H1/H1-J/RHF/supplement/CR2. Do not build provider blocks. Do not
mutate the fake-PQS route.

Current blocker:

```julia
:missing_independent_pqs_support_region_materializer
```

Current target constants that must not be treated as generated until proven:

```text
:atom_contact_core => 275
:shared_shell_1    => 578
:shared_shell_2    => 362
```

Audit questions:

1. Which existing code, if any, already generates the support rows or support
   descriptors for the H2 atom-contact core and shared shells without using
   WL/QW fixed-source coefficient matrices?
2. Can `atom_contact_core = 275` be derived from route geometry as two
   `5^3` atom cores plus a 25-row contact/midpoint slab? Where are those rows
   represented or representable?
3. Can `shared_shell_1 = 578` and `shared_shell_2 = 362` be generated by the
   shellification/lowering route spine, and what exact formulas or source
   surfaces produce those counts?
4. Which existing objects should own the support plan:
   shellification region object, terminal-lowering contract, PQS multilayer
   region plan, or a new compact private route support-plan object?
5. What exact minimal implementation pass should follow this audit?
6. What deletion candidates should be bundled with that implementation pass to
   preserve line-budget pressure?

Surfaces to inspect first:

- `src/pqs_source_box_route_driver_skeletons.jl`
- `src/pqs_source_box_diatomic_complete_core_shell.jl`
- `src/pqs_multilayer_shell_region_plan.jl`
- `src/pqs_multilayer_shell_source_plan.jl`
- `src/cartesian_shellification/`
- `src/cartesian_terminal_lowering/`
- `src/cartesian_route_core/`
- old response logs around passes 198-201 and 230-232 if needed

Forbidden interpretations:

- Do not call the support counts route-generated merely because pass 232 writes
  them.
- Do not use fake-PQS/WL `source.sequence.coefficient_matrix` slices as support
  authority.
- Do not treat a future retained count as known just because WL/QW had
  `(251, 98, 114)`.

Report:

- answer each audit question directly;
- name exact functions/types/files that can be reused;
- name exact missing function/type if implementation is absent;
- recommend the next pass boundary;
- list deletion/shrink candidates for the next implementation pass;
- include `git status --short --branch`;
- confirm no files were edited.

-- repo-manager@macmini
